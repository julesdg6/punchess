# Punchess Python Bot Template

Set env vars and run:

```bash
pip install -r requirements.txt
PUNCHESS_URL=http://localhost:2700 PUNCHESS_BOT_NAME=mybot python bot.py
```

Implement your own bot by replacing `choose_move(game_state)` to return a UCI move string (for example `e2e4`).
